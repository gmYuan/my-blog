# useState的state存储在哪里？

## 核心答案（来自源码）

函数组件的state保存在Fiber节点的memoizedState单链表上，每个Hook对应链表中的一个节点。

## 详细说明

**组件内部状态**：

在React函数组件中，我们可以使用useState和useReducer定义组件内部state，这个state其实是存储在VDOM上的，就是fiber节点上。每一个组件都有一个与之对应的js对象，即fiber节点。
