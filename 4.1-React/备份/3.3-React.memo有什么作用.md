# React.memo有什么作用

## 定义

memo 是一个 HOC (higher order component，高阶组件)。是个函数，接收组件作为参数，返回一个新的组件。

memo 允许你的组件在 props 没有改变的情况下跳过重新渲染。

## 用法

```jsx
const MemoizedComponent = memo(SomeComponent, arePropsEqual?)
```
