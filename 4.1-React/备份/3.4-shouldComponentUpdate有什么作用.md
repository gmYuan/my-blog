# shouldComponentUpdate有什么作用

## 定义

React 可以调用它来确定是否可以跳过重新渲染。

## 用法

```javascript
shouldComponentUpdate(nextProps, nextState, nextContext)
```

## 作用

让组件进入 bailout 阶段的方法之一。

通过比较更新前后的 props 和 state，返回布尔值来决定是否需要重新渲染组件。

如果返回 false，组件将跳过重新渲染，进入 bailout 阶段。
